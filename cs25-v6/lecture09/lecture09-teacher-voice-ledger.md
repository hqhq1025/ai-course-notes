# Lecture 09 Teacher-Voice Ledger

This ledger captures motivations, warnings, heuristics, jokes that expose common mistakes, and practical production experience that do not survive in slide text alone. Timestamps refer to the official Stanford recording.

| Time | Spoken point | Why it matters | Planned treatment in note |
|---|---|---|---|
| 00:00:56--00:01:55 | The course has emphasized model capability and production; this lecture follows the model to where people actually use it. Even training-oriented readers should learn the inference consequences of training choices. | Establishes inference as the completion of the model lifecycle rather than a secondary implementation detail. | Opening systems pivot and first `课堂提示`. |
| 00:02:42--00:04:39 | Training spends money to create weights; inference creates the surrounding system that can deliver value. Post-training itself now consumes substantial inference. | Connects product economics, RL generation, and serving infrastructure. | Motivation section and revenue/cost warning. |
| 00:04:39--00:05:59 | Inference spans applications, algebra, schedulers, networks, chips, and electrons; this breadth creates opportunities and makes optimization cross-layer. | Prevents readers from reducing serving to a single CUDA kernel. | Stack map and optimization-order thesis. |
| 00:06:00--00:07:35 | The speaker's claims come from operating inference at Modal, helping deployments across hundreds or thousands of GPUs, and running his own servers. | Bounds claims as production experience rather than universal theorem. | Source-context box beside slide 004. |
| 00:08:13--00:11:12 | Chatbot+, background agent, and data processor differ mainly in who consumes the output, how interactive the loop is, and how bursty the load can be. | Turns product labels into workload constraints. | Archetype table with consumer, latency, burstiness, and objective. |
| 00:11:17--00:13:15 | A workload definition and SLO are the handoff between product stakeholders and systems engineers; a concrete advisor shortened otherwise difficult conversations. | Gives an organizational reason for explicit metrics. | SLO contract subsection and checklist. |
| 00:13:20--00:13:52 | Peak-to-average traffic ratio matters more than many teams expect; variability and seasonality make serving harder. | Explains why mean QPS is not capacity planning. | Queueing/capacity warning. |
| 00:13:52--00:16:28 | Input/output token counts depend on users, harnesses, and the model; prefix reuse can trade storage for compute, but prompt harnesses can accidentally destroy cache locality. | Captures system-design control surfaces hidden behind user prompts. | TPQ and prefix-cache mechanism box. |
| 00:16:33--00:17:19 | TTFT and TPOT/ITL are easier to reason about than TTLT; tool calls make end-to-end latency empirical again. | Separates model-server latency from agent workflow latency. | Latency formula chain and agentic caveat. |
| 00:17:22--00:19:10 | Latency should usually be reasoned about per user and capacity per replica; scale independent replicas to total QPS rather than designing from aggregate traffic first. | Core production heuristic for benchmarking and autoscaling. | Replica-first algorithm and `课堂提示`. |
| 00:19:10--00:20:02 | Each archetype has a different figure of merit: tokens/s/user, TTLT, or megatokens/dollar. | Prevents optimizing the wrong scalar. | Objective table and worked examples. |
| 00:20:02--00:21:30 | Measure minimum latency with serial requests and maximum throughput with an all-at-once burst, then sweep the rates in between; the diagram is like an impulse response. | Provides a reproducible benchmark procedure and intuition for the latency-throughput frontier. | Benchmark pseudocode and frontier explanation. |
| 00:21:30--00:22:55 | A simulator makes p95/p99 token delays perceptible as stutter even when the median is fast. Tail latency measured per token compounds within one response. | Converts abstract percentiles into user experience. | Live-demo figure and tail-amplification formula. |
| 00:23:00--00:25:58 | Model choice belongs to workload definition. Efficiency-bound and capability-bound deployments occupy different engineering regimes; open-model fine-tunes are narrowing part of the gap. | Makes model selection a systems decision with an eval boundary. | Two-regime decision table. |
| 00:26:18--00:28:47 | Efficiency-bound workloads often fit one GPU; multi-GPU can still buy latency by exposing more memory pipes. Voice agents are a notable low-latency exception. | Explains why tensor parallelism can be used for latency even when capacity is sufficient. | Memory-bandwidth explanation and voice example. |
| 00:28:47--00:31:32 | Capability-bound workloads use large multi-GPU or multi-node models, often as orchestrators above cheaper subagents; tool calls make prefix reuse valuable and add latency from milliseconds to hours. | Connects model hierarchy, agent architecture, KV reuse, and end-to-end latency. | Orchestrator/subagent diagram and warning. |
| 00:32:12--00:33:12 | Artificial Analysis is a useful market map, but the intelligence score is still a compressed proxy; provider prices can be matched only with enough load to fill hardware. | Adds epistemic caution to cost-capability plots. | Plot reading box and utilization caveat. |
| 00:33:20--00:35:10 | An inference engine includes HTTP/RPC, tokenization, detokenization, CPU scheduling, and GPU workers. Tokenization can be CPU-intensive and parallelized. | Prevents the engine from being mistaken for only GPU kernels. | Process architecture glossary and pipeline listing. |
| 00:35:10--00:39:45 | TRT-LLM, vLLM, and SGLang differ in runtime overhead, compatibility, development culture, governance, and pace; simple reimplementations are useful for learning. | Reframes engine selection beyond benchmark bars. | Engine comparison table and study path. |
| 00:39:55--00:45:25 | Prefill moves model weights and performs many FLOPs; decode repeatedly moves many bytes for fewer FLOPs. Arithmetic intensity explains why the same model has two hardware regimes. | Provides the roofline bridge from workload to GPU selection. | Prefill/decode formulas and roofline box. |
| 00:42:45--00:45:00 | Recent data-center Tensor-Core GPUs matter because of HBM, power/form factor, NVLink/InfiniBand, and matrix hardware; most advertised FLOPs live in Tensor Cores. | First-use scaffolding for HBM, SRAM/shared memory, interconnect, and tensor cores. | Hardware glossary and bottleneck table. |
| 00:45:25--00:48:55 | CPUs usually miss latency because they lack HBM bandwidth and programmer-controlled shared memory; alternative accelerators often impose single-vendor software constraints. | Separates theoretical compute from deployable ecosystem. | Hardware alternatives warning. |
| 00:49:05--00:51:25 | Deployment is constrained by scarce GPUs, network latency, several definitions of utilization, and surprisingly frequent GPU failures. Independent replicas make inference more fault-tolerant than synchronized training, not failure-free. | Adds reliability and networking to serving design. | Deployment constraint section and failure-rate caveat. |
| 00:51:25--00:54:00 | Bursty demand creates a three-way tension: fixed capacity wastes GPUs, slow allocation hurts QoS, and fast automatic allocation is the target. | Sets up serverless allocation as a control problem. | Demand/allocation figure sequence and queueing model. |
| 00:54:00--00:56:55 | Fast startup requires more than getting a GPU: keep a cloud buffer, make container filesystems lazy and content-addressed, and restore process/GPU state instead of rebuilding it. | Explains the full cold-start critical path. | Three-layer startup architecture and checkpoint/restore warning. |
| 00:57:08--00:59:24 | Production bugs split into application, model-quality, and performance bugs. Tokenizers and chat templates are commonly wrong near model release. | Gives a concrete taxonomy and an actionable open-source contribution path. | Bug matrix and tokenizer test listing. |
| 00:59:24--01:01:27 | Observability asks whether logs contain enough state to reconstruct and fix a failure. Log token IDs, not only strings, and log more metrics than initially seem necessary. | Captures subtle Unicode/tokenizer failures and unknown-unknown debugging. | Observability box and structured log schema. |
| 01:01:27--01:02:55 | Evals are thankless, low-status, style-cramping work, but they outlive models and deployments. Start with a notebook or spreadsheet if necessary. | Preserves the strongest production lesson in the lecture. | `老师强调：Models are temporary, evals are forever` box. |
| 01:02:55--01:05:45 | Distinguish external RPS from internal QPS, track queues at every layer, split prefill/decode, and retain per-replica plus percentile metrics. | Specifies the minimum observability schema. | Metric taxonomy and queue diagnosis workflow. |
| 01:06:45--01:10:50 | Optimize big things first: speculation and quantization, then host overhead, then kernels. A six-times headline is workload-dependent, not a universal multiplier. | Establishes optimization order and guards against benchmark cargo culting. | Optimization funnel and claim boundary. |
| 01:07:45--01:10:50 | Speculative decoding works when a cheap drafter proposes tokens that the target can verify in parallel; acceptance, verification cost, and batch pressure decide the real gain. | Supplies mechanism missing from the slogan. | Speculation equations and pseudocode. |
| 01:10:50--01:13:20 | N-gram reuse, MTP, EAGLE, and DFlash occupy different draft-model designs; DFlash is the class the speaker is betting on based on observed results. | Separates method taxonomy from endorsement. | Comparison table with lecture-date paper boundary. |
| 01:13:20--01:16:10 | Quantization reduces bytes and may raise Tensor-Core throughput, but requires compatible hardware and application evals; attention and KV cache are harder than matmuls. | Makes quantization a full-stack correctness decision. | Quantization ladder and long-context warning. |
| 01:16:10--01:18:42 | Never block the GPU. Use CUDA Graphs and system profilers, then use ordinary host profilers such as py-spy; a Python dictionary change produced a double-digit gain. | Shows why host work precedes kernel rewriting. | Host-overhead section and py-spy case study. |
| 01:18:42--01:19:49 | Mature kernels are often near hardware limits and reusable. Pull out Nsight Compute only when percentage points matter, and reason on a whiteboard before measuring. | Prevents premature low-level optimization. | Kernel optimization decision gate. |
| 01:19:56--01:20:44 | Future serving may use deliberately lossy optimization, megakernels, and distinct prefill/decode accelerators. | Frames future work as an application-defined correctness tradeoff. | Future systems agenda and warning. |
| 01:20:50--01:22:19 | Agents can benchmark, configure, and monitor only after engineers build the correctness system, permissions, tools, and metrics. Cheap generation of bespoke engines could challenge general-purpose stacks. | Connects agentic software engineering to evals and observability instead of replacing them. | Final section, VibeServe snapshot, and synthesis checklist. |

## Visual audit note

- The complete 01:22:31 recording was sampled every five seconds, producing 990 frames and 17 reviewed contact sheets.
- The only deck-external teaching visual is the live token-timing simulator around 00:21:35--00:22:45; one representative frame is required.
- No independent whiteboard, question card, or unrepresented live coding/demo appears.
- Deck pages 070, 072, and 073 are not substantively presented in the recording; page 070 is a transition joke, page 072 is an unpresented CI/CL appendix, and page 073 is recruiting material.

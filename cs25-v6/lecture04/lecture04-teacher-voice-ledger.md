# Lecture 04 Teacher-Voice Ledger

| Time | Spoken point | Why it matters | Planned location |
|---|---|---|---|
| 00:00:48--00:02:03 | Tazi says the talk is based on the Ultra-Scale Playbook but adds newer MoE-scaling material. | Establishes that the deck is a practical systems snapshot, not a timeless taxonomy. | Opening and source boundary |
| 00:01:24--00:03:02 | Contemporary runs combine roughly trillion-parameter scale, tens of trillions of training tokens, and million-token contexts; the infrastructure problem is data movement, iteration time, memory, and checkpoint I/O together. | Prevents model size from becoming the only scaling variable. | Scaling pressure |
| 00:02:52--00:04:08 | Gradient accumulation solves global-batch scheduling only if the model already fits and sequential accumulation is fast enough. | Exposes the hidden assumptions behind the one-GPU baseline. | One-GPU baseline |
| 00:04:08--00:05:38 | The same parallelism ideas apply to GPUs/TPUs and to pretraining, post-training, distillation, and inference; even two accelerators can benefit. | Blocks the misconception that 5D parallelism matters only at thousand-GPU scale. | Scope and first-use glossary |
| 00:05:38--00:07:28 | Data parallelism replicates model state, shards data, and requires gradient synchronization before the optimizer step. | Gives the operational definition of DP. | DP mechanism |
| 00:07:28--00:09:58 | All-reduce can be viewed as reduce-scatter plus all-gather, and backward buckets let communication begin before the full backward pass finishes. | Connects collective algebra to overlap engineering. | DP collectives and timeline |
| 00:09:58--00:11:37 | Profiler timelines should reveal idle gaps; overlap is an empirical property, not something guaranteed by calling DDP. | Makes performance diagnosis part of the lesson. | Profiler reading |
| 00:11:37--00:13:45 | ZeRO-1 shards optimizer states; ZeRO-2 additionally discards gradient shards a rank will not update. | Distinguishes the stages by state ownership rather than memorized labels. | ZeRO-1/2 |
| 00:13:45--00:15:12 | Flattened sharding can conflict with optimizers such as Muon that need full tensor structure; tensor-preserving ownership matters. | Captures a modern implementation caveat absent from many old ZeRO summaries. | Optimizer-state glossary |
| 00:15:12--00:17:52 | ZeRO-3/FSDP all-gathers one module just in time, computes it, frees it, and prefetches the next module; the same pattern applies in backward. | Explains why parameter sharding can still be performant. | ZeRO-3 forward/backward |
| 00:17:52--00:20:10 | FSDP module size is a bucket-size tradeoff; FSDP2 preserves tensor structure and uses DTensor, making composition with other parallelisms easier. | Connects API evolution to the underlying scheduling problem. | FSDP code and timeline |
| 00:20:10--00:22:18 | Do not use ZeRO-3 by default: if ZeRO-1 fits, it usually communicates less and can be faster. | Preserves the speaker's strongest practical warning. | ZeRO decision rule |
| 00:22:18--00:23:42 | At large scale, hidden communication may reappear; hybrid sharding limits FSDP to a faster subgroup, and DP also stops scaling once global batch size is saturated. | Provides the transition from DP to TP. | DP limits |
| 00:23:42--00:26:02 | Tensor parallelism keeps the same data while splitting matrix weights and compute; column- then row-parallel matmuls restore the correct result with an all-reduce. | Gives TP's algebraic core. | TP matmul derivation |
| 00:26:02--00:28:15 | Backward mirrors the matrix factorization, under the important assumption that ranks begin with the same upstream gradient. | Makes a usually hidden correctness assumption explicit. | TP backward |
| 00:28:15--00:30:38 | MLP and attention are compositions of the same row/column-parallel matmul pattern, but communication now sits on the forward/backward critical path. | Links toy algebra to Transformer blocks. | TP MLP and attention |
| 00:30:38--00:32:42 | Replicated residual/layer-norm regions waste activation memory and rely on ranks doing identical work; sequence parallelism removes that fragile identity region. | Motivates TP+SP from activation accounting. | TP/SP motivation |
| 00:32:42--00:35:48 | Replacing an all-reduce with reduce-scatter plus all-gather alternates hidden-dimension and sequence-dimension sharding without increasing communication volume. | Explains why SP is not simply “extra communication.” | TP/SP properties |
| 00:35:48--00:37:52 | In backward, all-gather and reduce-scatter are adjoints/inverses in the schedule, so the added reduce-scatter synchronizes layer-norm gradients. | Resolves the layer-norm correctness question. | TP/SP backward |
| 00:37:52--00:39:58 | TP is normally kept inside a fast node or tightly connected domain because its collectives occur inside every block. | Ties algorithm choice to topology. | TP placement warning |
| 00:39:58--00:41:22 | Parallelism axes form process groups: data is sharded along DP and replicated along TP, while model dimensions are sharded along TP and replicated along DP. | Introduces device meshes as the composition language. | Combining DP and TP |
| 00:41:22--00:43:02 | Pipeline schedules determine when ranks execute forward/backward and exchange activations/gradients; AFAB fills the pipe but leaves a bubble. | Defines PP through schedules, not merely layer placement. | PP AFAB |
| 00:43:02--00:44:18 | 1F1B reduces activation pressure but does not eliminate the bubble; DualPipe sends work from both ends with much higher scheduler complexity. | Prevents overselling one scheduler. | PP scheduler comparison |
| 00:44:18--00:46:02 | PP communication is cheap and model-agnostic, but many micro-batches increase stored activations; checkpointing or CPU offload may be required. | Preserves the memory/throughput tradeoff. | PP pros/cons |
| 00:46:02--00:47:52 | Context parallelism shards sequence positions; Ring Attention exchanges K/V blocks and updates softmax online. | Gives CP's exact scope and mechanism. | CP and Ring Attention |
| 00:47:52--00:49:14 | CP is useful specifically when long-sequence activation memory is the bottleneck; it adds critical-path communication and helps little for short sequences. | Supplies the use-only-when-needed boundary. | CP decision rule |
| 00:49:14--00:51:42 | Expert parallelism shards experts, also shards data to avoid duplicate attention work, and uses all-to-all for token dispatch and combine. | Defines the non-orthogonality between EP and DP. | EP routing |
| 00:51:42--00:53:56 | Router-dependent message sizes create a CPU/GPU synchronization problem; DeepEP and HybridEP rely on recent networking/RDMA capabilities, making hardware part of the algorithm. | Captures the practical MoE bottleneck. | EP difficulty |
| 00:53:56--00:55:42 | A practical recipe overlaps one micro-batch's dispatch/combine with another micro-batch's pipeline work. | Shows that “5D” is scheduling composition, not five independent checkboxes. | EP+PP overlap |
| 00:55:42--00:57:18 | All five axes can be combined, but the closing recommendation is to use decision guides and consider the energy cost of thousands of accelerators. | Preserves the substantive closing message. | Synthesis and responsibility |
| 00:57:18--00:58:42 | MoE load balance can use an auxiliary loss or an auxiliary-loss-free router bias to prevent ranks from receiving highly uneven token counts. | Adds Q&A guidance not contained in the main deck. | Q&A: load balance |
| 00:58:42--00:59:34 | Correctly implemented parallel strategies should preserve the mathematical forward/backward result and therefore not change convergence merely by partitioning. | Separates systems equivalence from implementation bugs and numerical effects. | Q&A: convergence |
| 00:59:34--01:00:16 | CPU-side preprocessing and environment work can be hidden with preallocated data-loader workers, but the answer is workload-specific. | Extends the overlap mindset beyond GPU collectives. | Q&A: input pipeline |
| 01:00:16--01:01:34 | Automatic parallelism selection depends on model size, global batch, sequence length, network topology, and fast-link group size; there is no universal layout. | Ends with the central decision principle. | Q&A: topology-aware selection |

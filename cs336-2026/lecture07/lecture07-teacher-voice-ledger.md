# Lecture 07 Teacher-Voice Ledger

Primary source: `lecture07-slides.py`. This lecture is an executable walkthrough rather than a timed transcript, so source locations refer to `text(...)` nodes and nearby executable code. The ledger preserves the instructor's motivations, hierarchy, use-case distinctions, implementation choices, and explicit omissions.

| Source node / lines | Spoken or executable point | Why it matters | Where it appears in the note |
|---|---|---|---|
| `main()` lines 21--38 | Single-GPU and multi-GPU optimization share one theme: compute is far from data, so systems must orchestrate work to avoid transfer bottlenecks; multi-GPU is needed for capacity or speed. | Prevents parallelism from becoming a list of APIs and ties it to the locality model from Lectures 5--6. | Opening section, `统一主题`, and `课堂提示：这是同一个 locality 问题的放大版`. |
| `main()` lines 28--35 | The hierarchy runs from L1/shared memory and HBM through NVLink/NVSwitch to InfiniBand/Ethernet; fusion/tiling becomes replication/sharding at cluster scale. | Gives a stable way to place communication frequency onto hardware distance. | `泛化的层次结构` and the opening topology explanation. |
| `collective_operations()` lines 76--89 | Collectives are classic many-device communication patterns; broadcast/scatter/gather/reduce are foundations, while all-gather/reduce-scatter/all-reduce are training workhorses and all-to-all serves MoE. | Explains why the lecture teaches simple primitives before the operations used in modern training. | Collective glossary and `课堂提示：基础原语是为了构造训练工作马`. |
| `collective_operations()` lines 101--183 | Broadcast initializes ranks; scatter/gather/reduce are stepping stones; all-gather reconstructs parameters; reduce-scatter distributes reduced gradients; decomposing all-reduce enables ZeRO/FSDP flexibility. | Connects input/output layouts to actual model-state placement rather than rote terminology. | Collective diagrams, DDP/FSDP transition, and all-reduce decomposition discussion. |
| `collective_operations()` lines 185--206 | Balanced all-to-all resembles a distributed transpose, but unbalanced splits remain possible and should be avoided. | Exposes the systems reason MoE routing balance matters. | `all-to-all：MoE 的通信核心` and its load-balance warning. |
| `hardware()` lines 218--236 | Modern systems have fast node-local NVLink/NVSwitch, slower cross-node fabrics, RDMA paths that bypass CPU copies, and NCCL that detects topology and launches communication kernels. | Separates physical path, transport, and collective runtime responsibilities. | Hardware hierarchy, RDMA section, and two source-backed `课堂提示` boxes. |
| `torch_distributed()` lines 239--246 and `setup()` | PyTorch exposes one collective interface over Gloo for CPU and NCCL for GPU; the lecture uses multiprocessing so every rank executes the same program. | Makes clear that a simple function call is a multi-process protocol. | `torch.distributed` initialization and process-group discussion. |
| `benchmarking()` and `all_reduce()` lines 288--335 | Communication must be measured with warmup, CUDA synchronization, rank barriers, tensor bytes, world size, and an effective-bandwidth model. | Prevents launch-time measurements or peak-link specifications from being mistaken for training communication performance. | Benchmark section and `课堂提示：计时边界本身就是 benchmark 的一部分`. |
| `data_parallelism()` lines 375--387 | Local losses differ, all-reduced gradients agree, and therefore replicated parameters remain equal; FSDP/ZeRO replaces replication with all-gather and reduce-scatter. | States the invariant that makes data parallelism mathematically coherent and motivates the next lecture. | Data-parallel section and `课堂提示：允许 local loss 不同，不允许参数悄悄分叉`. |
| `tensor_parallelism()` lines 439--479 and `main()` line 69 | Each rank owns part of every layer and transfers activations; tensor parallelism requires very fast interconnects, while the minimal backward is left as homework. | Explains both the high communication frequency and the pedagogical boundary of the example. | Tensor-parallel section, backward caveat, and `课堂提示：TP 通常应留在最快互连域`. |
| `pipeline_parallelism()` lines 484--532 and `main()` line 70 | Each rank owns a subset of layers, microbatches reduce bubbles, but the minimal example does not overlap communication and computation. | Distinguishes the teaching implementation from a production schedule. | Pipeline section and `课堂提示：最小实现故意保留了生产系统要消除的 bubble`. |
| `main()` lines 59--72 | The lecture intentionally omits overlap, attention-specific details, and additional parallel dimensions; JAX can compile a declared sharding strategy, whereas PyTorch is used here to expose primitives; systems trade recomputation, memory, and communication. | Preserves the instructor's scope boundary and closing engineering framework. | Comparison section, JAX/PyTorch discussion, and recompute-memory-communication triangle. |

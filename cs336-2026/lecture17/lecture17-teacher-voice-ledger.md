# Lecture 17 Teacher-Voice Ledger

Primary sources: `transcript_timed.txt` and `lecture17-slides.py`. Timestamps refer to the public lecture transcript; executable source functions provide slide-aligned narration.

| Time / source node | Spoken point | Why it matters | Where it appears in the note |
|---|---|---|---|
| `00:00:53--00:01:55`, `main` | The north star is an omni model that accepts and produces arbitrary modality combinations, while this lecture focuses mainly on ingesting images. | Separates multimodal input support from native multimodal generation and prevents overclaiming current systems. | Opening `课堂提示` and the understanding/generation distinction. |
| `00:01:58--00:03:15`, `main` | Transformers operate on tokens, so images/audio/video must be converted into meaningful continuous or discrete token units rather than raw pixels. | Establishes tokenization as the unifying design question of the lecture. | `多模态的统一接口：把不同信号变成 tokens` and the comparison table. |
| `00:21:26--00:22:51`, `clip` / `siglip` | CLIP semantics are strong but image-classification oriented; it needs very large batches and a global softmax that is hard to decompose. | Connects the CLIP code shape to the systems motivation for SigLIP. | `课堂提示：CLIP 的 batch size 是 objective 的一部分`, CLIP/SigLIP code figures. |
| `llava`, source lines 137--145 | LLaVA uses a CLIP encoder, linear projector, and LM; alignment first trains the projector with both endpoints frozen, then fine-tuning updates projector and LM. | Shows that the projector is an interface whose meaning comes from staged training. | `LLaVA：Vision Encoder + Projector + Language Model`, shape-flow and freeze-schedule explanations. |
| `llava_onevision`, source lines 158--170 | Preserving resolution is crucial for OCR; AnyRes tiles images, while single-image, multi-image, and video inputs use different resolution/frame strategies to fit similar token budgets. | Makes multimodality a token-allocation problem rather than a fixed resize rule. | `Any-resolution tokenization`, modality-budget figure and read-figure box. |
| `llava_onevision`, source lines 172--191 | Data follows “quality over quantity” and training follows “easier to harder”; most progress comes from curated, synthesized task data. | Explains why source filtering, curriculum, and capability coverage matter as much as architecture. | `Data curriculum`, `课堂提示：quality over quantity，easier to harder`, and the data tables. |
| `llava_onevision`, source lines 181--187 | Diagram/chart skills transfer from single images to multi-image reasoning; OCR and relations transfer to GUI agents; visual prompting transfers to video. | Reframes cross-modal transfer in terms of shared latent skills. | `Cross-modal transfer` and all three transfer figures. |
| `00:47:10--00:48:26`, `qwen_vl` | Qwen-VL uses three stages: low-quality large-scale alignment with frozen LM, higher-quality task-specific all-parameter training, then instruction tuning with frozen visual encoder. | Makes the freeze schedule and data-quality progression explicit. | `Qwen-VL：固定视觉压缩与三阶段训练`, stage overview and stage-detail figures. |
| `00:49:31--00:52:50`, `qwen2_vl` | Dynamic resolution maps complex images to many tokens and tiny images to few; MRoPE tracks temporal, height, and width coordinates; capabilities span video, math/code, and function calling. | Links token budgets and positional encoding to concrete task coverage. | `Qwen2-VL：dynamic resolution 与 MRoPE`, architecture, MRoPE, and capability figures. |
| `00:57:13--00:59:21`, `qwen3_vl` | DeepStack injects visual features into multiple LM residual layers; pretraining progressively grows context from 8K to 32K to 256K; final results are strong but the pipeline is increasingly complex. | Prevents benchmark gains from being attributed to one architectural change. | `Qwen3-VL`, pretraining/results figures and cautious read-figure box. |
| `01:06:22--01:06:36`, `qwen3_vl` | Later Qwen models involve substantial data and data-mix work, but public details are limited. | Supports the lecture's restrained conclusion that architecture diagrams are incomplete evidence. | Qwen3 `课堂提示` and final data-pipeline box. |
| `01:07:59--01:13:58`, `chameleon` | Unified discrete image/text tokens are elegant, but image-token entropy causes instability and discretization loses OCR-level detail; QK norm and z-loss only partly mitigate it. | Captures the central elegance-versus-information-loss tradeoff. | `Chameleon`, VQ-VAE explanation, stability warning, and conclusion. |
| `01:15:22--01:17:13`, lecture conclusion | Understanding and generation demand different information granularity; modality weights must be balanced, and continuous encoders remain the practical default for understanding. | Connects visual token design to task density, loss balance, and current engineering choices. | Final comparison and `总结与延伸`. |

## Attribution Rule

- `课堂提示` is reserved for claims directly supported by the transcript or executable source.
- Figure-reading boxes may add engineering interpretation, but they should state when a benchmark or showcase does not isolate causal factors.

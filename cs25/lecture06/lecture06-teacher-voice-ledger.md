# CS25 Lecture 06 Teacher-Voice Ledger

Times refer to the current Stanford Online upload `wTZ3o36lXoQ`.

| Time / source node | Spoken point | Why it matters | Planned placement |
|---|---|---|---|
| 00:00:17--00:02:58 / T001 | Hand-designing an inductive bias for every sensor, scientific modality, and structured dataset will not scale; a unified architecture reduces fragile subsystem engineering. | Establishes the practical research problem, not just an architecture novelty. | Source audit and motivation. |
| 00:03:00--00:05:10 / T002 | Transformers are attractive because they are non-local, treat position as a feature, share weights, and map well to matrix-multiply hardware, but input scaling is poor. | Explains why the work starts from Transformers rather than inventing an unrelated module. | Transformer strengths and bottlenecks. |
| 00:05:10--00:09:30 / T003 | Non-locality and soft positional features preserve generality; convnets gain efficiency by hard-coding locality, while Transformers pay quadratic cost. | Frames the speed-versus-generality Pareto curve. | Inductive-bias comparison. |
| 00:10:40--00:16:16 / T004 | Image-sized self-attention is prohibitive because every input queries every other input; the goal is to separate the large input size from the smaller processing workspace. | Motivates the latent bottleneck mathematically. | Complexity derivation. |
| 00:16:16--00:19:00 / T005 | Cross-attention uses a small query array against a large input and scales linearly with input size when latent count is fixed. | Captures the central mechanism and its condition. | Cross-attention section. |
| 00:19:00--00:21:42 / T006 | Latents are randomly initialized and learned; repeated latent processing can share weights, and positional information may be concatenated rather than added. | Preserves implementation details missing from a simplified diagram. | Perceiver architecture and position encoding. |
| 00:21:42--00:23:12 / T007 | ViT patching and convolution are effective for images but introduce a grid-specific input assumption; DETR and Slot Attention show cross-attention already works as a structured bottleneck. | Prevents the false claim that Perceiver invented cross-attention or makes ViT obsolete. | Architecture comparison. |
| 00:23:12--00:30:01 / T008 | Q&A clarifies residual connections, the latent self-attention block, and a generality-versus-domain-specific-efficiency tradeoff; hierarchical cross-attention remained an open experiment. | Adds structural details and explicit limitations. | Q&A boxes around architecture. |
| 00:26:18--00:29:34 / T009 | Fourier features supply position without a hard convolutional neighborhood; concatenation to RGB worked better than projection plus addition. | Gives the empirical reason for a specific encoding choice. | Positional encoding and ImageNet. |
| 00:29:34--00:39:58 / T010 | The model remains competitive when pixels are permuted, showing it does not require the canonical 2D ordering; Q&A warns that recovering structure from data is costly. | Separates architectural possibility from data efficiency. | ImageNet evidence and warning. |
| 00:33:48--00:40:32 / T011 | Multimodal inputs can have different feature dimensions and semantics if modality and position information standardize them into one array. | Explains how “any data as a table” is implemented rather than asserted. | Multimodal featurization. |
| 00:40:32--00:41:34 / T012 | Basic Perceiver is naturally suited to a single low-dimensional prediction, while Perceiver IO adds a decoder cross-attention driven by output queries. | Motivates the IO extension. | Perceiver versus Perceiver IO. |
| 00:41:34--00:43:06 / T013 | Output queries specify what to predict and where; image autoencoding and multimodal tasks change query content rather than the core architecture. | Establishes the query-as-interface abstraction. | Query construction. |
| 00:43:44--00:45:38 / T014 | Tokenizers encode language-specific assumptions and can behave poorly across scripts; bytes provide a common low-level alphabet at the cost of longer sequences. | Preserves the motivation for token-free language. | Language section. |
| 00:45:38--00:48:00 / T015 | Perceiver IO applies masked language modeling and GLUE fine-tuning directly to bytes, remaining competitive with tokenized baselines under a FLOP budget. | Connects architecture to a concrete training protocol. | MLM/GLUE results. |
| 00:48:00--00:48:44 / T016 | Attention visualizations reveal byte patterns for punctuation, capitalization, word fragments, and repeated structures, but they are diagnostic rather than proof of linguistic concepts. | Adds interpretability with an evidence boundary. | Language attention figure. |
| 00:48:44--00:50:48 / T017 | Optical flow requires a dense motion vector for every pixel; the task has little realistic labeled data, so transfer from synthetic AutoFlow is central. | Explains why this is a hard structured-output test. | Optical-flow setup. |
| 00:50:32--00:51:14 / T018 | The authors expected weak inductive biases to overfit compared with RAFT, yet the model worked out of the box without handcrafted correlation hierarchy. | Preserves the surprising result and original expectation. | Optical-flow architecture. |
| 00:51:14--00:55:02 / T019 | EPE is evaluated on Sintel clean/final and KITTI without fine-tuning; Q&A explains how CGI or LiDAR-based ground truth is obtained and why dense labels are expensive. | Makes the metrics and transfer protocol interpretable. | Results and qualitative reading. |
| 00:55:02--00:58:53 / T020 | Perceivers are best viewed as a general fallback for domains with unclear structure; convnets can remain preferable for known domains, small-data behavior is unresolved, and joint multi-domain training is future work. | Gives the correct deployment boundary and research agenda. | Final synthesis. |

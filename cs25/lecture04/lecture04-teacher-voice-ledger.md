# CS25 Lecture 04 Teacher-Voice Ledger

| Time / source node | Spoken point | Why it matters | Planned placement |
|---|---|---|---|
| 00:35--03:13 / T001 | Transformer research had achieved stable scaling in perception; the real question is whether the same simplicity can extend from sensing to acting. | Prevents the lecture from reading as “apply GPT to RL” and restores the motivating systems claim. | Opening and “from perception to decision” section. |
| 03:13--07:00 / T002 | Offline RL deliberately removes new trial-and-error interaction; RL policies were much smaller and less stable than contemporary language models. | Explains why fixed logged data is both practically attractive and statistically dangerous. | RL/offline-RL background and scale-gap warning. |
| 07:00--13:22 / T003 | The model conditions on return-to-go, not only immediate reward; the target return is the inference-time control token. | This is the conceptual heart of Decision Transformer. | Sequence formulation and architecture. |
| 16:55--18:33 / T004 | Adding a timestep embedding shifts a token representation; concatenation would increase dimensionality and encode a different function. | Captures a concrete design-choice discussion absent from the slide bullets. | Forward-pass explanation and warning against treating embedding operations as interchangeable. |
| 18:59--20:00 / T004 | Non-Markov conditioning is intentional because history can disambiguate partial observations and action choice. | Corrects the common misconception that history is merely an expensive implementation detail. | Partial observability and context section. |
| 20:00--23:34 / T005 | The data-collection policy strongly conditions benchmark conclusions; the model is agnostic in code, but evidence is not agnostic in interpretation. | Separates algorithm interface from statistical support. | Dataset support and benchmark caveats. |
| 23:34--24:16 / T006 | MSE and cross-entropy are attractive not because they are novel, but because they are stable supervised objectives that avoid Bellman-style dynamic programming. | Explains the scaling thesis behind the method. | Training objective section. |
| 24:16--30:02 / T006 | Rollout is autoregressive generation initialized with a desired return; after each reward, the remaining return is decremented and the context window is updated. | Turns the architecture into an executable inference algorithm. | Rollout pseudocode and worked example. |
| 30:00--38:12 / T007 | Target return and realized return are different quantities; evaluation must use rewards actually obtained from the environment. | Prevents circular evaluation and “the model gets whatever score we request” confusion. | Return-conditioning experiment and warning box. |
| 38:12--45:00 / T008 | Extreme target returns tend to saturate rather than improve indefinitely; occasional extrapolation is inconsistent across environments. | Sets an honest boundary on controllability and OOD generalization. | Return-conditioning read-figure explanation. |
| 40:00--45:00 / T008 | Calling return conditioning “multitask” is defensible only as selecting different behaviors; it is not automatically the same as task identity or broad transfer. | Preserves the classroom challenge to an over-broad claim. | Terminology and interpretation warning. |
| 45:03--50:00 / T009 | Percent behavior cloning is a surprisingly strong baseline; Decision Transformer separates most clearly when data is limited. | Forces comparison against a simple, high-signal baseline instead of only sophisticated RL algorithms. | Behavior-cloning section and table explanation. |
| 50:00--55:12 / T010 | The context-length ablation asks whether history truly matters; `K=1` approaches a purely current-state policy. | Connects architecture to an empirical test of non-Markov conditioning. | Context-length section. |
| 55:12--1:00:04 / T011 | Sparse reward does not make exploration disappear in general; the offline log supplies successful/failed trajectories, while online cold start remains hard. | Distinguishes sequence credit assignment from exploration. | Sparse reward and online-RL warning. |
| 1:00:04--1:05:03 / T012 | Replacing action prediction with return prediction turns the same causal model into a critic whose belief changes across Key-to-Door phases. | Shows the architecture is a sequence model, not only a fixed actor head. | Critic reinterpretation section. |
| 1:05:03--1:11:30 / T013 | The closing thesis is simplicity and stable optimization, followed by multimodal, multitask and multi-agent extensions rather than a claim that RL is solved. | Keeps the conclusion proportional to the evidence. | Summary and future-work section. |
| 1:11:42--1:16:36 / T014 | Naively adding epsilon-greedy or Boltzmann sampling does not automatically yield online RL; exploration/exploitation details do not transfer unchanged. | A practical failure mode that is easy to miss from the paper abstract. | Online extension warning. |
| 1:16:36--1:18:22 / T015 | Discounted return is compatible with Decision Transformer, and finite context may still fail on very long horizons. | Corrects the misconception that the method eliminates discounting or memory limits. | Discounted-return formula and limitations. |
| 1:18:22--1:20:37 / T016 | CQL-style pessimism and Q-learning objectives could be added, but the original experiment intentionally tests whether a simple sequence-modeling reframing is already competitive. | Separates conceptual contribution from possible hybrid engineering. | Final synthesis and extensions. |

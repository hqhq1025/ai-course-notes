# Lecture 40 Teacher-Voice Ledger

| Time / source node | Spoken point | Why it matters | Planned location |
|---|---|---|---|
| 00:01:06--00:02:48 | The talk is intentionally non-exhaustive, does not teach diffusion or flow matching in full, and treats the discussed backbones as mostly agnostic to the exact generative path. | Prevents the note from pretending to be a complete diffusion survey and separates scheduler math from network parameterization. | Opening scope |
| 00:02:48--00:04:44 | Modern generation is moving beyond isolated images toward native multimodal media, but text-to-image remains a clear architecture laboratory. | Connects the examples to the broader CS25 theme. | Motivation |
| 00:04:44--00:06:25 | Diffusion is iterative rather than one-shot; text conditioning turns the denoising trajectory into a controllable generation process. | Supplies the speaker's preferred mental model. | Diffusion intuition |
| 00:05:00--00:09:30 | A production text-to-image system is a graph of text encoders, noisy latents, a denoiser, scheduler, and VAE rather than one monolithic model. | Corrects the “diffusion model equals one neural network” misconception. | System components |
| 00:09:30--00:13:15 | Diffusion and flow matching differ in path construction, but the lecture focuses on the learnable network that predicts noise/velocity rather than the scheduler. | Defines what claims can transfer across objectives. | Objective versus backbone |
| 00:14:44--00:19:45 | UNets became giant collections of custom ResNet, transformer, sampling, and convolution blocks, making architecture maintenance and scaling awkward. | Motivates DiT through software and scaling complexity, not fashion. | Giant UNet |
| 00:19:45--00:21:54 | UViT moves toward Transformer structure, while DiT can inherit mainstream Transformer innovations such as QK-Norm, GQA, positional encodings, and parallel MLPs. | Preserves the ecosystem-transfer argument. | UViT to DiT |
| 00:21:54--00:24:53 | Original DiT changes less than expected from ViT: patchification and encoder blocks remain, while timestep, class conditioning, and output decoding are added. | Gives the core architecture compression. | Original DiT |
| 00:24:53--00:29:30 | Conditioning enters each block through adaptive LayerNorm rather than cross-attention; scale, shift, and gates are regressed from combined class/time conditions. | Explains the actual mechanism behind the diagrams. | adaLN derivation |
| 00:29:30--00:32:56 | Zero initialization makes each block initially close to identity, and adaLN-Zero is both important and computationally efficient. | Captures an implementation detail with large training impact. | Initialization |
| 00:32:56--00:36:50 | Text-to-image needs richer prompt embeddings and cross-attention; PixArt uses a large text encoder and shares adaLN parameters to reduce compute. | Shows which parts of class-conditioned DiT do and do not transfer. | PixArt-α |
| 00:36:50--00:40:45 | Compact models can perform strongly, but architecture slides do not cover the distinct difficulty of training data, optimization, and post-training. | Keeps result claims scoped. | PixArt results and warning |
| 00:40:45--00:45:30 | Ultra-high-resolution latent grids make quadratic self-attention prohibitive; efficiency must target token mixing without losing text conditioning. | Motivates SANA from resource accounting. | Quadratic cost |
| 00:45:30--00:49:49 | SANA uses linear self-attention while retaining cross-attention, plus Mix-FFN and other compensation mechanisms; speed claims must be read with quality tradeoffs. | Prevents “linear attention solves everything” simplification. | SANA |
| 00:49:49--00:54:58 | Text and image representations occupy different spaces; MMDiT gives each modality separate adaLN and QKV before joint attention so they can co-evolve without forced symmetry. | Explains why simple concatenation may inherit cross-modal bias. | MMDiT |
| 00:54:58--00:57:31 | Full MMDiT is expensive, so production models mix multimodal and regular DiT blocks or alternate modality updates. | Adds FLOP-aware architecture judgment. | MMDiT variants |
| 00:57:31--01:01:38 | Q\&A covers autoregressive alternatives, weak automatic metrics, GANs for one-shot low-latency generation, and the extra temporal consistency burden in video. | Preserves practical evaluation and deployment tradeoffs. | Evidence and evaluation |
| 01:01:45--01:03:00 | Adaptive LayerNorm is explained as condition-dependent feature modulation on top of normalization, not merely a stabilization trick. | Clarifies a common classroom confusion. | adaLN intuition |
| 01:03:04--01:04:45 | MMDiT's separate QKV paths are motivated by modality differences, but the answer is explicitly heuristic rather than a proof of optimality. | Keeps the architecture rationale honest. | MMDiT evidence boundary |
| 01:05:00--01:08:43 | DiT-Air asks how much can be shared: adaLN, QKVO, and MLP sharing improve efficiency but can trade away quality; MMDiT is not always necessary. | Preserves the lecture's strongest simplification result. | DiT-Air |
| 01:08:47--01:10:40 | Structural controls with spatial correspondence differ from subject-driven or editing conditions that lack direct pixel alignment. | Prevents ControlNet-style intuition from being overgeneralized. | Structural control |
| 01:10:40--01:11:44 | Video adds 3D token interactions, temporal consistency, and severe attention cost; RoPE and efficiency work such as LTX-Video are practical directions. | Extends image architecture reasoning to video. | Video generation |
| 01:11:44--01:13:20 | In-context generation requires architectures that can jointly represent examples and targets; current designs remain too inefficient for broad use. | Frames Playground v3, FuseDiT, and OmniGen as an open architecture problem. | Next generation |
| 01:13:20--01:14:23 | The talk did not cover MoE, training, post-training, preference alignment, or evaluation in depth; these are distinct research programs. | Defines the final evidence boundary and future syllabus. | Final synthesis |

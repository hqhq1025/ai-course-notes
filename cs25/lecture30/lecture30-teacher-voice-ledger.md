# Lecture 30 Teacher-Voice Ledger

| Time | Spoken point | Why it matters | Planned placement |
|---|---|---|---|
| 00:01:19--00:02:49 | Williams approaches neural language models from mathematics, physics, and quantitative linguistics, so the talk is intentionally less standard than a normal Transformer lecture. | Explains why the lecture starts from statistical structure and explicit formulas rather than scaling recipes. | Source framing |
| 00:02:54--00:05:06 | Standard self-attention re-dimensionalizes vectors so dot products become useful; the alternative proposal instead learns a matrix that converts existing vector comparisons into feature weights. | Supplies the central conceptual contrast hidden behind the first slide's compact equations. | Alternative attention |
| 00:05:15--00:05:43 | The proposed mechanism is compatible with conventional projections; the talk omits them to study whether the alternative weighting mechanism is useful by itself. | Prevents the false claim that SAFFU mathematically forbids Q/K projections. | Architecture boundary |
| 00:05:43--00:07:10 | Avoiding projection parameters supports a near-shallow architecture and reduces random parameter initialization, but it also imposes stronger assumptions on vector dimensions and organization. | Connects efficiency motivation to the proposal's representational constraints. | SAFFU definition |
| 00:08:03--00:10:44 | Multiple cloned attention heads with the same targets can be seen through the Lottery Ticket lens: random initialization makes their roles ambiguous and may spend training on parameters that never become useful. | Captures the speaker's motivation without presenting Lottery Ticket as proof against multi-head attention. | Initialization motivation |
| 00:11:14--00:12:40 | Dimensionality reduction remains essential for large vocabularies, yet random embeddings sit far from the output loss and are expensive to stabilize. | Explains why the talk needs a deterministic embedding scheme before explicit layer initialization. | Embedding bottleneck |
| 00:12:53--00:15:45 | Bit-cipher assigns unique low-dimensional bit vectors by frequency order; it is deterministic, avoids random initialization, and was the practical bridge into the later SAFFU work. | Preserves the historical development and the distinction between identity coding and contextual training. | Bit-cipher |
| 00:15:55--00:18:09 | The single-layer warm start comes from generalized input-output co-occurrences, a logarithm, and a normalization related to context size; the co-occurrence need not be a traditional text window. | Turns the theorem slide into an operational explanation. | Explicit softmax solution |
| 00:18:13--00:19:44 | Extending the formula to attention requires hidden targets for which positions should receive weight; the team's analysis treats attention as shaping vectors into what the next layer expects. | States the compositional difficulty and the team's proposed solution. | Hidden attention targets |
| 00:19:48--00:21:11 | Non-random embeddings initialize attention matrix $W$, its output initializes feed-forward matrix $U$, and label embeddings help define lower-dimensional targets before the final output matrix. | Provides the bottom-up warm-start sequence needed to read the architecture diagram. | SAFFU pipeline |
| 00:21:15--00:22:43 | The speaker characterizes the single-layer computation as distributed outer-product accumulation followed by normalization and a log transform, with all parameters beginning at zero rather than random values. | Adds concrete implementation intuition and the role of softmax's exponential inverse. | Algorithm box |
| 00:22:47--00:24:24 | Warm starts immediately lower perplexity and keep a better trajectory under the same learning rate; early stopping can therefore occur sooner and at a lower perplexity. | Explains what the warm/cold plot actually compares. | Warm-start evidence |
| 00:24:28--00:27:00 | In response to student confusion, Williams repeats that this is an alternative attention mechanism; standard Q/K projections are useful when dimensions differ, while the proposal focuses on same-basis comparisons. | Preserves a live clarification absent from the slide. | Architecture comparison |
| 00:29:14--00:31:20 | The MNIST experiment is meant to show that the explicit softmax initialization is not limited to linguistic inputs; non-negative image features can use the same single-layer solution. | Supports limited cross-domain generality without claiming a complete vision architecture. | Image experiment |
| 00:31:20--00:32:15 | The exact priming number for non-unit features is approximated by the average input norm; the speaker notes the missing graph and points to the paper for the evidence. | Flags an approximation and a slide omission instead of overstating the formula. | Priming-number caveat |
| 00:32:20--00:34:55 | Longer fixed windows cost more but do not always carry more information; the proposal treats context complexity as a property of the data rather than a single universal block length. | Motivates multiple contexts and dynamic batching. | Context modeling |
| 00:34:55--00:37:34 | Multi-context SAFFU branches can use different widths while sharing a common output; this is possible because the modified attention matrix can be subset by context length. | Explains the diagram's multiple $W_b,U_b$ paths. | Multi-context design |
| 00:37:34--00:39:41 | Naive bit assignments preserve token identity but do not remove embedding brittleness; radial co-occurrence statistics are proposed to add correlation structure. | Separates deterministic identity vectors from semantically useful embeddings. | Embedding refinement |
| 00:39:41--00:41:48 | If embedding vectors are frozen, first-layer pairwise comparisons can be precomputed and loaded; updating embeddings invalidates that cache and restores part of the training cost. | States the central condition behind the near-shallow speed claim. | Cached comparisons |
| 00:41:57--00:42:33 | Low randomness lets a model fit small data quickly, but knowing a few thousand tokens well does not imply general language ability. | Prevents the note from equating fast memorization with broad generalization. | Small-data warning |
| 00:42:38--00:45:55 | Packing fills unused context with unrelated documents and needs masking; dynamic length batching keeps short documents short and uses only the relevant subset of modified attention parameters. | Gives the speaker's context-faithfulness argument and its computational motivation. | Packing versus dynamic context |
| 00:46:14--00:47:31 | The team does not yet know how the architecture scales to trillions of tokens or RLHF; stress tests show speedups when vector comparisons are cached and expected quadratic growth when they are not. | Separates measured engineering behavior from speculative LLM-scale claims. | PLM evidence boundary |
| 00:47:35--00:49:04 | The `potato` model deliberately targets scratch training on user speech at the edge; the team's first objective is not another chatbot but a capability conventional large models do not emphasize. | Explains why tiny configurations are not merely toy downsizing. | PLM configurations |
| 00:49:09--00:50:19 | The medium model warm-starts on BabyLM-scale data, then spends most time in iterative training; 100 million tokens is used as a developmentally plausible language-exposure scale. | Connects the training curve to its data regime. | Medium PLM training |
| 00:50:22--00:51:35 | Freezing vectors after warm start speeds optimization and stabilizes the cache, while thawing them later can improve the final model at additional cost. | Explains the three-stage warm/freeze/thaw schedule. | Training stages |
| 00:52:48--00:54:10 | The GPT-2/A100 comparison is used as a rough reference point; direct comparisons are difficult because context, vocabulary, implementation, and initialization differ. | Keeps the slide's headline number from becoming a controlled benchmark claim. | Training-speed caveat |
| 00:54:16--00:55:21 | The team has mostly stress-tested small PLMs and asks whether stable explicit initialization can support transfer-like behavior from application data alone. | Frames the next section as an open research question. | Small-PLM capabilities |
| 00:55:21--00:58:34 | The switch task removes generic pretraining: a local loop records speech, transcribes it, lets a user operate the switch when needed, and learns from those paired directives. | Supplies the application-only training loop. | Voice interaction |
| 00:58:34--01:00:10 | Data collection is intentionally interactive: the system listens, asks for action only when uncertain, stores the transcript/action pair, and continues learning locally. | Shows how labels arise without a pre-existing instruction corpus. | Smart data collection |
| 01:00:10--01:02:48 | Switch commands are represented as dialogue; the smallest character-level `potato` PLM learned directives in about 20 minutes, while larger configurations trade more compute for better predictions. | Grounds the edge example in its actual data format and scale. | Dialogue and configurations |
| 01:02:48--01:04:08 | Positive prediction performance appears only after enough examples exist; inference on Le Potato still takes seconds, so the system is feasible but frustrating rather than production-ready. | Preserves both the result and the latency limitation. | Emergent performance |
| 01:04:11--01:06:50 | Binary control is only a starting point; conversational reply, phone actions, better bit-cipher vectors, hybrid attention, larger evaluations, and BabyLM participation remain unfinished work. | Captures the lecture's own closing limitations. | Future work |
| 01:07:53--01:09:37 | The warm-versus-cold single-layer plot contains no attention at all; concatenated one-hot features reduce the need for reweighting, and the attention solution came later. | Corrects an easy misreading of the evidence plot. | Q&A evidence clarification |
| 01:09:42--01:11:46 | Vision/IoT extensions need explicit initializations for convolution and other activations; otherwise randomly initialized unsupported layers reintroduce noise. | Limits cross-domain generalization to the layer types actually derived. | Q&A layer coverage |
| 01:14:33--01:15:25 | The softmax solution can accept mixed non-negative feature types, so image-captioning is conceivable if the separate feature pipelines satisfy the assumptions. | Offers a bounded multimodal extension rather than a demonstrated result. | Q&A multimodal extension |
| 01:15:29--01:16:14 | Non-negativity enters through logarithms of generalized co-occurrences; negative inputs require a different derivation rather than a trivial implementation tweak. | Makes a hidden mathematical assumption explicit. | Q&A assumptions |
| 01:16:20--01:19:00 | Dynamic context and document packing have not been compared in a matched large-model experiment; packing is an efficiency technique, while the speaker argues dynamic blocks preserve the actual context model. | Labels the comparison as an untested research judgment. | Q&A context caveat |
| 01:19:02--01:19:41 | A public SAFFU implementation was not yet available because evaluation infrastructure for nonstandard architectures still had to be built. | Prevents the note from inventing a repository or claiming a mature toolkit. | Reproducibility boundary |

## In-note obligations

- Use at least 26 explicit teacher-voice markers.
- Preserve the distinction among standard Transformer facts, paper derivations, slide experiments, speaker interpretations, and open future work.
- Define SAFFU, PLM, bit-cipher, warm start, cold start, priming number, explicit solution, caching, packing, and dynamic context on first use.
- Integrate the Q&A into the corresponding technical sections instead of appending an undigested transcript summary.
